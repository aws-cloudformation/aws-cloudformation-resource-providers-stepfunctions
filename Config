package.AWSCloudFormationResourceProvidersStepFunctions = {
    interfaces = (1.0);

    deploy = {
        generic = true;
    };

    build-environment = {
        chroot = basic;
        network-access = open; # TODO: Remove when CFN validates tokens w/o network access
    };
    
    build-system = copy-and-brazil-gradle;
    build-tools = {
        1.0 = {
            AWSCloudFormationRPDK = 1.1;
            AWSCloudFormationRPDKJavaPluginTool = 1.1;
            BrazilGradle = 4.x;
            BrazilPath = 1.1;
            JDK8 = 1.0;
            UluruBuild = 1.0;
        };
    };

    # Use runtime-dependencies for when you want to bring in additional
    # packages when deploying.
    # Use dependencies instead if you intend for these dependencies to
    # be exported to other packages that build against you.
    dependencies = {
        1.0 = {
            AWSCloudFormationRPDKJavaPlugin = 1.1;
            AWSStepFunctionsJavaClient = 1.11.x;
        };
    };

    test-dependencies = {
        1.0 = {
            AssertJ = 3.2.x;
            JUnit5 = 5.x;
            Mockito = 2.22.x;
            Mockito-junit-jupiter = 2.22.x;
        };
    };

};
